apiVersion: v1
kind: Service
metadata:
  name: test-nodeport
  namespace: prod
spec:
  selector:
    app: grafana
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32002
  type: NodePort